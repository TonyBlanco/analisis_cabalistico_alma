# CHANGELOG - Sesión 17 Diciembre 2025 (10:33)
Estado de la sesión: ESTABILIZACIÓN / FREEZE
## Resumen de Cambios de las Últimas ~5 Horas

Este documento registra TODOS los cambios realizados en la aplicación durante la sesión de trabajo del 17 de diciembre de 2025.

---

## 🔧 BACKEND CHANGES

### 1. `backend/api/serializers.py`

**Cambio**: Añadido campo `phone` al `UserProfileDetailSerializer`

```python
fields = [
    "legal_full_name",
    "full_name",
    "phone",  # ← NUEVO
    "birth_date",
    ...
]
```

**Razón**: El frontend esperaba este campo para mostrar el teléfono del usuario en la página de cuenta.

---

### 2. `backend/api/urls.py`

**Cambio**: Añadido alias para endpoint de perfil de cuenta

```python
# Alias para cuenta de paciente (usa el mismo endpoint que profile/me/)
path('account/profile/', UserProfileMeView.as_view(), name='account_profile'),
```

**Razón**: Compatibilidad con llamadas del frontend a `/api/account/profile/`

---

### 3. `backend/api/views.py`

**Cambios**:

#### 3.1 `UpdateProfileView` - Añadido `force_geocode`
```python
force_geocode = request.data.get('force_geocode', False)
location_changing = city_changing or country_changing or force_geocode
```

**Razón**: Permitir forzar re-geocodificación de coordenadas cuando los datos guardados son incorrectos.

#### 3.2 `EmailOrUsernameAuthToken` - Errores de login específicos

```python
# Antes: 400 Bad Request genérico
# Después: Errores específicos:
- 404: user_not_found
- 401: invalid_password (incluye email para recuperación)
- 403: account_inactive
- 400: validation
```

**Razón**: Feedback preciso al usuario sobre qué falló en el login.

---

### 4. Nuevos archivos Backend (no modificados en esta sesión pero relevantes)

- `backend/api/patient_profile_views.py` - Vistas de perfil de paciente
- `backend/api/patient_status_views.py` - Vistas de estado de paciente
- `backend/api/migrations/0032_add_profile_tracking_fields.py`
- `backend/api/migrations/0033_add_therapy_status.py`
- `backend/api/migrations/0034_bioemotionaldictionaryentry_and_more.py`

---

## 🎨 FRONTEND CHANGES

### 1. `tonyblanco-app/app/(dashboard)/layout.tsx`

**Cambios**:
- Simplificada lógica de renderizado de sidebar para pacientes
- Eliminada lógica de `isSharedRoute` que causaba duplicación de sidebar
- Las rutas de paciente ahora TODAS van bajo `/dashboard/patient/`

**Antes**:
```tsx
if (isSharedRoute && realUserRole === 'patient') {
  // Renderizaba PatientSidebar para /dashboard/account
}
```

**Después**:
- Eliminado este bloque
- Todas las rutas de paciente manejadas por `patient/layout.tsx`

---

### 2. `tonyblanco-app/app/(dashboard)/dashboard/account/page.tsx`

**Cambios**:
- Corregidas dependencias del `useEffect` que impedían carga del perfil:
  ```tsx
  // Antes
  }, [router]);
  
  // Después
  }, [router, roleLoaded, role]);
  ```
- Añadido logging de debug:
  ```tsx
  console.log('📋 Loading profile for role:', role);
  console.log('📋 Profile loaded:', profile);
  ```
- Añadido botón "🔄 Recalcular coordenadas desde ciudad" para forzar geo-resolución

**Razón**: El perfil no se cargaba porque el efecto no se re-ejecutaba cuando `roleLoaded` cambiaba a `true`.

---

### 3. `tonyblanco-app/app/(dashboard)/dashboard/patient/account/page.tsx`

**Estado**: CREADO NUEVO (restaurado según documentación)

**Contenido**:
- Página de cuenta específica para pacientes
- Usa `/api/profile/me/` directamente
- Campos editables: nombre legal, teléfono
- Campos de solo lectura: datos de nacimiento (gestionados por terapeuta)
- Muestra estado del consentimiento
- Botón de cerrar sesión

**Razón**: La documentación (`PANEL_PACIENTE_DEFINITIVO.md`) especifica que la ruta debe ser `/dashboard/patient/account`, no `/dashboard/account`.

---

### 4. `tonyblanco-app/app/(dashboard)/dashboard/patient/components/PatientSidebar.tsx`

**Cambio**: Corregida ruta de "Cuenta"

```tsx
// Antes
{ href: '/dashboard/account', label: 'Cuenta', icon: User },

// Después
{ href: '/dashboard/patient/account', label: 'Cuenta', icon: User },
```

**Razón**: Seguir estructura de rutas definida en documentación.

---

### 5. `tonyblanco-app/app/(dashboard)/dashboard/patient/results/[id]/page.tsx`

**Estado**: CREADO NUEVO

**Contenido**:
- Ruta dinámica para resultados individuales de paciente
- Muestra placeholder "Los resultados detallados estarán disponibles próximamente"
- Navegación de vuelta a lista de resultados

**Razón**: Evitar 404 al acceder a URLs como `/dashboard/patient/results/2`

---

### 6. `tonyblanco-app/app/(public)/login/page.tsx`

**Cambios mayores**:

#### 6.1 Limpieza de token al entrar
```tsx
useEffect(() => {
  clearAuthState();
}, []);
```

#### 6.2 Rediseño visual completo
- Layout de pantalla dividida (branding izquierda, formulario derecha)
- Toggle de visibilidad de contraseña
- Indicadores de carga animados

#### 6.3 Mensajes de error específicos
```tsx
switch (err.response?.error) {
  case 'user_not_found':
    setError({ type: 'user_not_found', message: '...' });
    break;
  case 'invalid_password':
    setError({ type: 'invalid_password', message: '...', email: err.response.email });
    break;
  // etc.
}
```

#### 6.4 Formulario de recuperación de contraseña integrado
- Aparece automáticamente cuando el error es `invalid_password`
- Pre-llena el email

**Razón**: Mejorar UX de login con feedback específico y diseño profesional.

---

### 7. `tonyblanco-app/app/(public)/layout.tsx`

**Cambio**: Simplificado para no interferir con login

```tsx
// Antes
<div className="centering-wrapper">
  {children}
</div>

// Después
<>{children}</>
```

**Razón**: El wrapper estaba rompiendo el diseño de pantalla completa del login.

---

### 8. `tonyblanco-app/app/not-found.tsx`

**Cambio**: Convertido a client component con navegación inteligente

**Funcionalidad**:
- Detecta si el usuario está logueado (localStorage)
- Redirige al dashboard correcto según rol:
  - `admin` → `/dashboard/admin`
  - `therapist` → `/dashboard/therapist`
  - `personal` → `/dashboard/personal`
  - `patient` → `/dashboard/patient`
  - No logueado → `/login`
- Botón "Volver atrás" usando `router.back()`
- Indicador de sesión activa

**Razón**: Evitar que usuarios logueados sean redirigidos al login en páginas 404.

---

### 9. `tonyblanco-app/components/ui/Toast.tsx`

**Estado**: CREADO NUEVO

**Contenido**:
- Sistema de notificaciones toast profesional
- Tipos: success, error, warning, info
- Auto-dismiss configurable
- Animaciones de entrada/salida
- Context provider (`ToastProvider`)
- Hook `useToast()`

**Uso**:
```tsx
const toast = useToast();
toast.success('Guardado correctamente');
toast.error('Error al guardar');
toast.warning('Selecciona un paciente');
```

---

### 10. `tonyblanco-app/components/Providers.tsx`

**Estado**: CREADO NUEVO

**Contenido**:
```tsx
'use client';
import { ToastProvider } from '@/components/ui/Toast';

export function Providers({ children }) {
  return (
    <ToastProvider>
      {children}
    </ToastProvider>
  );
}
```

**Razón**: Wrapper de providers client-side para toda la aplicación.

---

### 11. `tonyblanco-app/app/layout.tsx`

**Cambio**: Integrado `Providers`

```tsx
<body>
  <Providers>
    {children}
  </Providers>
</body>
```

**Razón**: Habilitar sistema de toast globalmente.

---

### 12. `tonyblanco-app/app/globals.css`

**Cambio**: Añadida animación para toasts

```css
@keyframes slide-in-right {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

.animate-slide-in-right {
  animation: slide-in-right 0.3s ease-out;
}
```

---

### 13. `tonyblanco-app/components/TestCatalogSection.tsx`

**Cambios**:
- Reemplazado `alert()` con toast/modal profesional
- Modal de éxito personalizado tras asignar test
- Mensaje incluye: "El paciente ha recibido un email con las instrucciones"

```tsx
// Antes
alert('Test asignado correctamente');

// Después
setShowSuccessModal(true);
setLastAssignedTest(test);
```

---

### 14. `tonyblanco-app/lib/api.ts`

**Cambios**:

#### 14.1 `UserProfileData` interface
```tsx
export interface UserProfileData {
  // ...existing fields...
  force_geocode?: boolean; // NUEVO
}
```

#### 14.2 `apiRequest` - Mejor propagación de errores
```tsx
if (!response.ok) {
  const error = await response.json().catch(() => ({}));
  console.error('❌ API Error:', { status: response.status, error });
  const errorWithResponse = new Error(errorMsg);
  (errorWithResponse as any).status = response.status;
  (errorWithResponse as any).response = error; // NUEVO
  throw errorWithResponse;
}
```

**Razón**: Permitir que el frontend acceda a los detalles del error del backend.

---

## 📁 ESTRUCTURA DE RUTAS CONFIRMADA

### Paciente (según documentación)
```
/dashboard/patient/
├── page.tsx          (Inicio)
├── tests/            (Tests asignados)
├── results/          (Resultados)
│   └── [id]/         (Resultado individual) ← NUEVO
├── resources/        (Recursos)
├── process/          (Proceso)
└── account/          (Cuenta) ← RESTAURADO
```

### Terapeuta
```
/dashboard/therapist/
├── page.tsx          (Workspace principal)
├── components/       
│   └── TherapistSidebar.tsx
├── layout.tsx
└── tests/
```

---

## ⚠️ PERMISOS Y SEGURIDAD

### No se modificaron:
- Role guards (`role-guards.ts`)
- Permisos de backend
- Ownership checks
- Validaciones de consentimiento

### Se mantiene:
- Pacientes NO pueden editar datos de nacimiento (solo terapeuta)
- Límite de 2 cambios de nombre legal
- Geo-resolución obligatoria en backend (no en frontend)

---

## 🔌 ENDPOINTS API UTILIZADOS

| Endpoint | Método | Descripción | Usado por |
|----------|--------|-------------|-----------|
| `/api/profile/me/` | GET | Perfil del usuario autenticado | account/page, patient/account |
| `/api/profile/me/` | PATCH | Actualizar perfil | account/page, patient/account |
| `/api/account/profile/` | GET/PATCH | Alias de profile/me | account/page (paciente) |
| `/api/auth/token/` | POST | Login con errores específicos | login/page |

---

## 🐛 BUGS CORREGIDOS

1. **Perfil no cargaba**: Dependencias faltantes en useEffect
2. **Login no limpiaba token**: Añadido clearAuthState() en mount
3. **Doble sidebar en paciente**: Eliminada lógica de rutas compartidas
4. **404 redirigía a login**: Ahora detecta rol y redirige a dashboard
5. **Coordenadas incorrectas persistían**: Añadido force_geocode
6. **Alertas nativas**: Reemplazadas con Toast system
7. **Errores de login genéricos**: Ahora específicos por tipo

---

## 📋 ARCHIVOS MODIFICADOS (Lista completa)

### Backend
- `backend/api/serializers.py`
- `backend/api/urls.py`
- `backend/api/views.py`

### Frontend - App
- `tonyblanco-app/app/(dashboard)/layout.tsx`
- `tonyblanco-app/app/(dashboard)/dashboard/account/page.tsx`
- `tonyblanco-app/app/(dashboard)/dashboard/patient/account/page.tsx` (NUEVO)
- `tonyblanco-app/app/(dashboard)/dashboard/patient/components/PatientSidebar.tsx`
- `tonyblanco-app/app/(dashboard)/dashboard/patient/results/[id]/page.tsx` (NUEVO)
- `tonyblanco-app/app/(public)/layout.tsx`
- `tonyblanco-app/app/(public)/login/page.tsx`
- `tonyblanco-app/app/globals.css`
- `tonyblanco-app/app/layout.tsx`
- `tonyblanco-app/app/not-found.tsx`

### Frontend - Components
- `tonyblanco-app/components/ui/Toast.tsx` (NUEVO)
- `tonyblanco-app/components/Providers.tsx` (NUEVO)
- `tonyblanco-app/components/TestCatalogSection.tsx`

### Frontend - Lib
- `tonyblanco-app/lib/api.ts`

---

## ✅ ESTADO FINAL

- **Página de cuenta de paciente**: ✅ Funcional en `/dashboard/patient/account`
- **Sistema de Toast**: ✅ Implementado globalmente
- **Login profesional**: ✅ Con errores específicos
- **Página 404 inteligente**: ✅ Redirige según rol
- **Geo-resolución forzada**: ✅ Disponible vía force_geocode
- **Estructura de rutas**: ✅ Conforme a documentación

---

*Generado: 17 Diciembre 2025, 10:33*
*Versión de sesión: Corrección de carga de perfil y restauración de estructura de rutas*

## 🧠 Nota sobre datos legacy

Usuarios creados antes del 17/12/2025 pueden:
- no tener ciudad / país
- no tener coordenadas válidas
- requerir re-guardado del perfil por terapeuta

Este comportamiento es esperado y no se considera bug.
