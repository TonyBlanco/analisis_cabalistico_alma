## Summary

Replaces the stub at `/dashboard/patient/process` with a client-facing vertical timeline that shows:

- Assigned/pending/completed explorations (reuses PR #34 patientPendingTests logic)
- Therapist activity milestones (natal chart, cabalistic work, shared astrology reports)
- Warm Spanish copy — no scores, clinical bands, or instrument names

## Backend (additive)

- `GET /api/patient/process-milestones/` — patient-safe therapist milestones
- Registers `GET /api/analysis-records/my-results/` (view existed, route was missing)

## Tests

- `patientProcess.test.ts` — 4/4 (pending/completed states, CTAs, sort)

## Merge note

Rebased on current main. Depends on PR #34 pending-tests util (already merged). Touches patient portal only plus additive API.

## TODO (annotated in API)

- arbol_vida_workspace patient read
- Full tarot SWM workspace as patient milestone