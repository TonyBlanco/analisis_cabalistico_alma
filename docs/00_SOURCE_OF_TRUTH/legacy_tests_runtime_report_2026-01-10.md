# Runtime Verification Report — Legacy Tests

**Date:** 2026-01-10
**Script run:** `verify_legacy_filters.py`
**Purpose:** Produce runtime status of tests (active/inactive, legacy markers) from the local DB.

---

## Notes about runtime environment

- To allow the script to run in this environment (Django settings include `symbolic.*` entries), a temporary import shim was created in the workspace temp folder:
  - Temporary path created: `.tmp_symbolic_stub/` with minimal `symbolic/72_angels/__init__.py` and `symbolic/swm_v3/__init__.py` files.
  - A temporary settings wrapper `temp_settings.py` was created in the same temp folder and used via `DJANGO_SETTINGS_MODULE=temp_settings`. This wrapper imports `backend.core.settings` and filters out `symbolic.*` entries from `INSTALLED_APPS` at runtime.
  - No repository files under source control were modified. The shim exists only in the temp directory and was used solely for this verification run.

- Command executed:

```powershell
$env:PYTHONPATH='d:\analisis_cabalistico_alma\.tmp_symbolic_stub;'+$env:PYTHONPATH
$env:DJANGO_SETTINGS_MODULE='temp_settings'
python d:\analisis_cabalistico_alma\verify_legacy_filters.py
```

---

## Script Output (summary)

- Total de tests activos: **19**
- Tests con `_legacy_app_backup` en descripción: **3**
- Tests con `No ejecutable` en descripción: **3**
- Total de tests legacy excluidos: **3**
- Tests que aparecen en catálogo (16):

  - anxiety-state-trait: Ansiedad — Estado y rasgo
  - bdi-ii: BDI-II - Inventario de Depresión de Beck
  - dbg-test-1: DBG Test
  - dbg-test-2: DBG Test
  - insomnia: Insomnia — Descanso y hábitos
  - lock-test-8dc2dc: Lock Test
  - lock-test-904d25: Lock Test
  - lock-test-ca1983: Lock Test
  - lock-test2-9839aa: Lock Test
  - nutrition: Alimentación — Relación y hábitos
  - past-lives: Vidas Pasadas – Exploración de Memorias del Alma
  - scl90: SCL-90 — Screening Holístico
  - screening-general: Screening Psicológico General
  - smoke-test-1: Smoke Test 1
  - stress-regulation: Estrés — Carga y regulación
  - wellness: Wellness Assessment

- Detalle de tests legacy excluidos (3 items):
  - `bai`: BAI
    • Marcadores: `_legacy_app_backup`, `No ejecutable`
  - `gad-7`: GAD-7
    • Marcadores: `_legacy_app_backup`, `No ejecutable`
  - `phq-9`: PHQ-9
    • Marcadores: `_legacy_app_backup`, `No ejecutable`

- Verificación adicional (Wellness)
  - `wellness: Wellness Assessment` — is_active: True — Legacy?: NO

---

## Observations & Implications

- The runtime DB indicates **19 active tests** and that **3 legacy tests are explicitly excluded** by markers (`_legacy_app_backup` and `No ejecutable`). Notably, `phq-9` appears in the excluded list during this run (contradiction with previously observed static `tests_catalog_status.json`).

- This result shows the catalog status in the repo (`tests_catalog_status.json`) is a **snapshot** and may differ from DB runtime state; therefore governance decisions must rely on runtime verification for final decisions.

- Actionable next steps for Governance (recommended):
  - Create a ticket to reconcile `tests_catalog_status.json` (repo snapshot) with runtime DB status and record canonical source of truth (DB vs docs). Owner: Governance / Product.
  - If archival or migration is decided for legacy tests, schedule a migration plan and acceptance criteria (backup, communication, testing). Owner: Governance.

---

## Files and Evidence

- Verification script: `verify_legacy_filters.py`
- Runtime evidence captured during execution (stdout embedded above)
- Repo snapshot file: `tests_catalog_status.json`

---

*Report generated by automated audit run; this file is read-only and intended for governance tracking.*
