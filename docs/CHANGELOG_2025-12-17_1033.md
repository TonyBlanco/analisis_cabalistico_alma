> ΓÜá∩╕Å DOCUMENTO HIST├ôRICO  
> Este changelog refleja el estado al 17/12/2025.  
> La fuente de verdad vigente es la auditor├¡a del 18/12/2025.

# CHANGELOG - Sesi├│n 17 Diciembre 2025 (10:33)
Estado de la sesi├│n: ESTABILIZACI├ôN / FREEZE
## Resumen de Cambios de las ├Ültimas ~5 Horas

Este documento registra TODOS los cambios realizados en la aplicaci├│n durante la sesi├│n de trabajo del 17 de diciembre de 2025.

---

## ≡ƒöº BACKEND CHANGES

### 1. `backend/api/serializers.py`

**Cambio**: A├▒adido campo `phone` al `UserProfileDetailSerializer`

```python
fields = [
    "legal_full_name",
    "full_name",
    "phone",  # ΓåÉ NUEVO
    "birth_date",
    ...
]
```

**Raz├│n**: El frontend esperaba este campo para mostrar el tel├⌐fono del usuario en la p├ígina de cuenta.

---

### 2. `backend/api/urls.py`

**Cambio**: A├▒adido alias para endpoint de perfil de cuenta

```python
# Alias para cuenta de paciente (usa el mismo endpoint que profile/me/)
path('account/profile/', UserProfileMeView.as_view(), name='account_profile'),
```

**Raz├│n**: Compatibilidad con llamadas del frontend a `/api/account/profile/`

---

### 3. `backend/api/views.py`

**Cambios**:

#### 3.1 `UpdateProfileView` - A├▒adido `force_geocode`
```python
force_geocode = request.data.get('force_geocode', False)
location_changing = city_changing or country_changing or force_geocode
```

**Raz├│n**: Permitir forzar re-geocodificaci├│n de coordenadas cuando los datos guardados son incorrectos.

#### 3.2 `EmailOrUsernameAuthToken` - Errores de login espec├¡ficos

```python
# Antes: 400 Bad Request gen├⌐rico
# Despu├⌐s: Errores espec├¡ficos:
- 404: user_not_found
- 401: invalid_password (incluye email para recuperaci├│n)
- 403: account_inactive
- 400: validation
```

**Raz├│n**: Feedback preciso al usuario sobre qu├⌐ fall├│ en el login.

---

### 4. Nuevos archivos Backend (no modificados en esta sesi├│n pero relevantes)

- `backend/api/patient_profile_views.py` - Vistas de perfil de paciente
- `backend/api/patient_status_views.py` - Vistas de estado de paciente
- `backend/api/migrations/0032_add_profile_tracking_fields.py`
- `backend/api/migrations/0033_add_therapy_status.py`
- `backend/api/migrations/0034_bioemotionaldictionaryentry_and_more.py`

---

## ≡ƒÄ¿ FRONTEND CHANGES

### 1. `tonyblanco-app/app/(dashboard)/layout.tsx`

**Cambios**:
- Simplificada l├│gica de renderizado de sidebar para pacientes
- Eliminada l├│gica de `isSharedRoute` que causaba duplicaci├│n de sidebar
- Las rutas de paciente ahora TODAS van bajo `/dashboard/patient/`

**Antes**:
```tsx
if (isSharedRoute && realUserRole === 'patient') {
  // Renderizaba PatientSidebar para /dashboard/account
}
```

**Despu├⌐s**:
- Eliminado este bloque
- Todas las rutas de paciente manejadas por `patient/layout.tsx`

---

### 2. `tonyblanco-app/app/(dashboard)/dashboard/account/page.tsx`

**Cambios**:
- Corregidas dependencias del `useEffect` que imped├¡an carga del perfil:
  ```tsx
  // Antes
  }, [router]);
  
  // Despu├⌐s
  }, [router, roleLoaded, role]);
  ```
- A├▒adido logging de debug:
  ```tsx
  console.log('≡ƒôï Loading profile for role:', role);
  console.log('≡ƒôï Profile loaded:', profile);
  ```
- A├▒adido bot├│n "≡ƒöä Recalcular coordenadas desde ciudad" para forzar geo-resoluci├│n

**Raz├│n**: El perfil no se cargaba porque el efecto no se re-ejecutaba cuando `roleLoaded` cambiaba a `true`.

---

### 3. `tonyblanco-app/app/(dashboard)/dashboard/patient/account/page.tsx`

**Estado**: CREADO NUEVO (restaurado seg├║n documentaci├│n)

**Contenido**:
- P├ígina de cuenta espec├¡fica para pacientes
- Usa `/api/profile/me/` directamente
- Campos editables: nombre legal, tel├⌐fono
- Campos de solo lectura: datos de nacimiento (gestionados por terapeuta)
- Muestra estado del consentimiento
- Bot├│n de cerrar sesi├│n

**Raz├│n**: La documentaci├│n (`PANEL_PACIENTE_DEFINITIVO.md`) especifica que la ruta debe ser `/dashboard/patient/account`, no `/dashboard/account`.

---

### 4. `tonyblanco-app/app/(dashboard)/dashboard/patient/components/PatientSidebar.tsx`

**Cambio**: Corregida ruta de "Cuenta"

```tsx
// Antes
{ href: '/dashboard/account', label: 'Cuenta', icon: User },

// Despu├⌐s
{ href: '/dashboard/patient/account', label: 'Cuenta', icon: User },
```

**Raz├│n**: Seguir estructura de rutas definida en documentaci├│n.

---

### 5. `tonyblanco-app/app/(dashboard)/dashboard/patient/results/[id]/page.tsx`

**Estado**: CREADO NUEVO

**Contenido**:
- Ruta din├ímica para resultados individuales de paciente
- Muestra placeholder "Los resultados detallados estar├ín disponibles pr├│ximamente"
- Navegaci├│n de vuelta a lista de resultados

**Raz├│n**: Evitar 404 al acceder a URLs como `/dashboard/patient/results/2`

---

### 6. `tonyblanco-app/app/(public)/login/page.tsx`

**Cambios mayores**:

#### 6.1 Limpieza de token al entrar
```tsx
useEffect(() => {
  clearAuthState();
}, []);
```

#### 6.2 Redise├▒o visual completo
- Layout de pantalla dividida (branding izquierda, formulario derecha)
- Toggle de visibilidad de contrase├▒a
- Indicadores de carga animados

#### 6.3 Mensajes de error espec├¡ficos
```tsx
switch (err.response?.error) {
  case 'user_not_found':
    setError({ type: 'user_not_found', message: '...' });
    break;
  case 'invalid_password':
    setError({ type: 'invalid_password', message: '...', email: err.response.email });
    break;
  // etc.
}
```

#### 6.4 Formulario de recuperaci├│n de contrase├▒a integrado
- Aparece autom├íticamente cuando el error es `invalid_password`
- Pre-llena el email

**Raz├│n**: Mejorar UX de login con feedback espec├¡fico y dise├▒o profesional.

---

### 7. `tonyblanco-app/app/(public)/layout.tsx`

**Cambio**: Simplificado para no interferir con login

```tsx
// Antes
<div className="centering-wrapper">
  {children}
</div>

// Despu├⌐s
<>{children}</>
```

**Raz├│n**: El wrapper estaba rompiendo el dise├▒o de pantalla completa del login.

---

### 8. `tonyblanco-app/app/not-found.tsx`

**Cambio**: Convertido a client component con navegaci├│n inteligente

**Funcionalidad**:
- Detecta si el usuario est├í logueado (localStorage)
- Redirige al dashboard correcto seg├║n rol:
  - `admin` ΓåÆ `/dashboard/admin`
  - `therapist` ΓåÆ `/dashboard/therapist`
  - `personal` ΓåÆ `/dashboard/personal`
  - `patient` ΓåÆ `/dashboard/patient`
  - No logueado ΓåÆ `/login`
- Bot├│n "Volver atr├ís" usando `router.back()`
- Indicador de sesi├│n activa

**Raz├│n**: Evitar que usuarios logueados sean redirigidos al login en p├íginas 404.

---

### 9. `tonyblanco-app/components/ui/Toast.tsx`

**Estado**: CREADO NUEVO

**Contenido**:
- Sistema de notificaciones toast profesional
- Tipos: success, error, warning, info
- Auto-dismiss configurable
- Animaciones de entrada/salida
- Context provider (`ToastProvider`)
- Hook `useToast()`

**Uso**:
```tsx
const toast = useToast();
toast.success('Guardado correctamente');
toast.error('Error al guardar');
toast.warning('Selecciona un paciente');
```

---

### 10. `tonyblanco-app/components/Providers.tsx`

**Estado**: CREADO NUEVO

**Contenido**:
```tsx
'use client';
import { ToastProvider } from '@/components/ui/Toast';

export function Providers({ children }) {
  return (
    <ToastProvider>
      {children}
    </ToastProvider>
  );
}
```

**Raz├│n**: Wrapper de providers client-side para toda la aplicaci├│n.

---

### 11. `tonyblanco-app/app/layout.tsx`

**Cambio**: Integrado `Providers`

```tsx
<body>
  <Providers>
    {children}
  </Providers>
</body>
```

**Raz├│n**: Habilitar sistema de toast globalmente.

---

### 12. `tonyblanco-app/app/globals.css`

**Cambio**: A├▒adida animaci├│n para toasts

```css
@keyframes slide-in-right {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

.animate-slide-in-right {
  animation: slide-in-right 0.3s ease-out;
}
```

---

### 13. `tonyblanco-app/components/TestCatalogSection.tsx`

**Cambios**:
- Reemplazado `alert()` con toast/modal profesional
- Modal de ├⌐xito personalizado tras asignar test
- Mensaje incluye: "El paciente ha recibido un email con las instrucciones"

```tsx
// Antes
alert('Test asignado correctamente');

// Despu├⌐s
setShowSuccessModal(true);
setLastAssignedTest(test);
```

---

### 14. `tonyblanco-app/lib/api.ts`

**Cambios**:

#### 14.1 `UserProfileData` interface
```tsx
export interface UserProfileData {
  // ...existing fields...
  force_geocode?: boolean; // NUEVO
}
```

#### 14.2 `apiRequest` - Mejor propagaci├│n de errores
```tsx
if (!response.ok) {
  const error = await response.json().catch(() => ({}));
  console.error('Γ¥î API Error:', { status: response.status, error });
  const errorWithResponse = new Error(errorMsg);
  (errorWithResponse as any).status = response.status;
  (errorWithResponse as any).response = error; // NUEVO
  throw errorWithResponse;
}
```

**Raz├│n**: Permitir que el frontend acceda a los detalles del error del backend.

---

## ≡ƒôü ESTRUCTURA DE RUTAS CONFIRMADA

### Paciente (seg├║n documentaci├│n)
```
/dashboard/patient/
Γö£ΓöÇΓöÇ page.tsx          (Inicio)
Γö£ΓöÇΓöÇ tests/            (Tests asignados)
Γö£ΓöÇΓöÇ results/          (Resultados)
Γöé   ΓööΓöÇΓöÇ [id]/         (Resultado individual) ΓåÉ NUEVO
Γö£ΓöÇΓöÇ resources/        (Recursos)
Γö£ΓöÇΓöÇ process/          (Proceso)
ΓööΓöÇΓöÇ account/          (Cuenta) ΓåÉ RESTAURADO
```

### Terapeuta
```
/dashboard/therapist/
Γö£ΓöÇΓöÇ page.tsx          (Workspace principal)
Γö£ΓöÇΓöÇ components/       
Γöé   ΓööΓöÇΓöÇ TherapistSidebar.tsx
Γö£ΓöÇΓöÇ layout.tsx
ΓööΓöÇΓöÇ tests/
```

---

## ΓÜá∩╕Å PERMISOS Y SEGURIDAD

### No se modificaron:
- Role guards (`role-guards.ts`)
- Permisos de backend
- Ownership checks
- Validaciones de consentimiento

### Se mantiene:
- Pacientes NO pueden editar datos de nacimiento (solo terapeuta)
- L├¡mite de 2 cambios de nombre legal
- Geo-resoluci├│n obligatoria en backend (no en frontend)

---

## ≡ƒöî ENDPOINTS API UTILIZADOS

| Endpoint | M├⌐todo | Descripci├│n | Usado por |
|----------|--------|-------------|-----------|
| `/api/profile/me/` | GET | Perfil del usuario autenticado | account/page, patient/account |
| `/api/profile/me/` | PATCH | Actualizar perfil | account/page, patient/account |
| `/api/account/profile/` | GET/PATCH | Alias de profile/me | account/page (paciente) |
| `/api/auth/token/` | POST | Login con errores espec├¡ficos | login/page |

---

## ≡ƒÉ¢ BUGS CORREGIDOS

1. **Perfil no cargaba**: Dependencias faltantes en useEffect
2. **Login no limpiaba token**: A├▒adido clearAuthState() en mount
3. **Doble sidebar en paciente**: Eliminada l├│gica de rutas compartidas
4. **404 redirig├¡a a login**: Ahora detecta rol y redirige a dashboard
5. **Coordenadas incorrectas persist├¡an**: A├▒adido force_geocode
6. **Alertas nativas**: Reemplazadas con Toast system
7. **Errores de login gen├⌐ricos**: Ahora espec├¡ficos por tipo

---

## ≡ƒôï ARCHIVOS MODIFICADOS (Lista completa)

### Backend
- `backend/api/serializers.py`
- `backend/api/urls.py`
- `backend/api/views.py`

### Frontend - App
- `tonyblanco-app/app/(dashboard)/layout.tsx`
- `tonyblanco-app/app/(dashboard)/dashboard/account/page.tsx`
- `tonyblanco-app/app/(dashboard)/dashboard/patient/account/page.tsx` (NUEVO)
- `tonyblanco-app/app/(dashboard)/dashboard/patient/components/PatientSidebar.tsx`
- `tonyblanco-app/app/(dashboard)/dashboard/patient/results/[id]/page.tsx` (NUEVO)
- `tonyblanco-app/app/(public)/layout.tsx`
- `tonyblanco-app/app/(public)/login/page.tsx`
- `tonyblanco-app/app/globals.css`
- `tonyblanco-app/app/layout.tsx`
- `tonyblanco-app/app/not-found.tsx`

### Frontend - Components
- `tonyblanco-app/components/ui/Toast.tsx` (NUEVO)
- `tonyblanco-app/components/Providers.tsx` (NUEVO)
- `tonyblanco-app/components/TestCatalogSection.tsx`

### Frontend - Lib
- `tonyblanco-app/lib/api.ts`

---

## Γ£à ESTADO FINAL

- **P├ígina de cuenta de paciente**: Γ£à Funcional en `/dashboard/patient/account`
- **Sistema de Toast**: Γ£à Implementado globalmente
- **Login profesional**: Γ£à Con errores espec├¡ficos
- **P├ígina 404 inteligente**: Γ£à Redirige seg├║n rol
- **Geo-resoluci├│n forzada**: Γ£à Disponible v├¡a force_geocode
- **Estructura de rutas**: Γ£à Conforme a documentaci├│n

---

*Generado: 17 Diciembre 2025, 10:33*
*Versi├│n de sesi├│n: Correcci├│n de carga de perfil y restauraci├│n de estructura de rutas*

## ≡ƒºá Nota sobre datos legacy

Usuarios creados antes del 17/12/2025 pueden:
- no tener ciudad / pa├¡s
- no tener coordenadas v├ílidas
- requerir re-guardado del perfil por terapeuta

Este comportamiento es esperado y no se considera bug.
