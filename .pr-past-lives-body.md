## Summary

Expands the **past-lives** symbolic exploration (additive, deterministic):

- **11 Likert sections** (s1–s11), 55 items (+ guided reflection)
- **Intro card** — gilgul / Tikun as symbolic lens (not literal claims)
- **Guided reflection** — 3 optional fields persisted in `open_reflection` (string or object; scorer normalizes)
- **Description fix** — removes misleading "numerología kármica" copy

## Scorer

`compute_past_lives` only: extended `sections` dict + `_normalize_past_lives_open_reflection`. Output contract unchanged.

## Tests

- `pastLives.test.ts` — 4/4 (config shape, reflection builder)
- `test_diagnostics.PastLivesDiagnosticTests` — legacy retrocompat + s7 influence + guided object
- Local smoke: `compute_past_lives OK`

## Prod note

Run migration `0105_update_past_lives_description` on deploy.